# 链表

## 掌握：

### 单链表
```
head
 ↓
[10] → [20] → [30] → NULL
```

## 实现：
```text
-  创建 
-  遍历 
-  插入 
-  删除 
-  查找 
-  修改 
-  链表长度 
-  链表逆序
```

### 结点的定义
```c
typedef struct Node
{
    int data; //这个节点真正保存的数据
    struct Node* next; //此时结点的typedef还未定义完成，仍需struct
    //next 是一个指针，保存下一个节点的地址。
}Node;
```
```text
┌──────────────┐
│ data │ next  │
│  10  │  ●────┼────→ 下一个节点
└──────────────┘
```
#### 练习1：创建一个 Node，让 p 指向它，并把 data 设置为 10，next 设置为 NULL。





